[project]
name = "sefia-examples"
version = "0.1.0"
description = "Usage examples for the sefia framework."
requires-python = ">=3.11"
dependencies = [
    "typer[all]",
    "python-dotenv",
    "rich",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
