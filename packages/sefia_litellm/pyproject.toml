[project]
name = "sefia_litellm"
version = "0.1.0"
description = "LiteLLM adapter for the sefia framework."
requires-python = ">=3.11"
dependencies = [
    "sefia",
    "litellm>=1.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sefia_litellm"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.uv.sources]
sefia = { workspace = true }

[dependency-groups]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "pytest-mock>=3.12.0"]
