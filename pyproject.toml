[tool.uv.workspace]
members = [
    "packages/sefia",
    "packages/sefia_litellm",
    "examples"
]

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "ruff>=0.15.16",
]
